db {
  jdbcUrl = "jdbc:postgresql://127.0.0.1:5433/dacs"
    user = "postgres"
    password = ""
    driver = "org.postgresql.Driver"
    table = "filter_rules"
}
kafka {
  consumer {
    bootstrap.servers = "localhost:9092"
    group.id = "test_group_consumer"
    auto.offset.reset = "earliest"
    topic = "test_topic_in"

  }
  producer {
    bootstrap.servers = "localhost:9092"
    topic = "test_topic_out"
  }

}
application {
  updateIntervalSec = 30
}