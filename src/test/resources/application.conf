db {
  jdbcUrl = "postgres://postgres:postgrespw@localhost:32768"
  user = "postgres"
  password = "postgrespw"
  driver = ""
}
kafka {
  consumer {
    bootstrap.servers = "localhost:9092"
    group.id = "test_group_consumer"
    auto.offset.reset = "latest"
  }
  producer {
    bootstrap.servers = "localhost:9092"
  }

}
application {
  updateIntervalSec = 30
}